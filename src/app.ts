import { topics, createSession, prepareClaim, confirmClaim, latest, gaps, packageSession, demoSession, uid, nextTopic, type TopicId, type Session } from './domain.js';
import { load, save, putFile, getFile, deleteFile, type Store } from './storage.js';
import { contextKind, kindLabels, activeFacets, coverage, candidateFacets, localQuestion, currentQuestion, advance, deferCurrent, type ContextKind } from './flow.js';
import { buildMarkdown } from './markdown.js';
const root = document.querySelector<HTMLDivElement>('#app')!;
const esc = (v: unknown) => String(v ?? '').replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]!));
let store: Store;
try { store = load(); } catch { root.innerHTML = '<main><h1>No pudimos recuperar tu sesión.</h1><p>Los datos originales se conservaron. Abre otro perfil del navegador para iniciar una sesión nueva.</p></main>'; throw Error('Storage recovery required'); }
let s = store.sessions.find(x => x.id === store.activeId) ?? store.sessions[0];
let aiEnabled = false; let aiLabel = 'Comprobando conexión…';
let recorder: MediaRecorder | undefined; let busy = false; let saved = true; let toastTimer: ReturnType<typeof setTimeout>; let urls: string[] = [];
function notify(text: string) { const el = document.querySelector('#toast')!; el.textContent = text; el.classList.add('visible'); clearTimeout(toastTimer); toastTimer = setTimeout(() => el.classList.remove('visible'), 4500); }
function persist() { s.updatedAt = new Date().toISOString(); s.markdown = buildMarkdown(s); try { save(store); saved = true; } catch { saved = false; notify('No se pudo guardar. Conserva esta pestaña abierta y copia tu brief.'); } const el = document.querySelector('#saved'); if (el) el.textContent = saved ? '✓ Guardado en este navegador' : 'No guardado'; }
function render() {
  urls.forEach(URL.revokeObjectURL); urls = [];
  const activeIds = new Set(activeFacets(s).map(f => f.topic));
  const visibleTopics = topics.filter(t => activeIds.has(t.id));
  const count = visibleTopics.filter(t => latest(s, t.id)).length;
  root.innerHTML = `<header><a class="brand" href="/" aria-label="GNX inicio"><b>✳</b> gnx <small>PROJECT STUDIO</small></a><div class="header-actions"><span>● Espacio local</span><button id="new">＋ Nuevo contexto</button></div></header><div class="shell"><aside class="rail"><p class="eyebrow">TU ESPACIO DE TRABAJO</p><label class="sr" for="sessions">Contexto activo</label><select id="sessions">${store.sessions.map(x => `<option value="${x.id}" ${x.id === s.id ? 'selected' : ''}>${esc(x.title)}${x.demo ? ' · ejemplo' : ''}</option>`).join('')}</select><nav aria-label="Vistas"><button data-view="interview" class="${s.view === 'interview' ? 'active' : ''}">◉ Inception <small>01</small></button><button data-view="brief" class="${s.view === 'brief' ? 'active' : ''}">▤ Contexto 360 · MD <small>02</small></button></nav><div class="rail-note"><span>✳</span><h3>Las buenas preguntas<br>abren posibilidades.</h3><p>No necesitas tenerlo todo claro para comenzar.</p></div><details class="demos"><summary>Explorar un ejemplo</summary><button data-demo="0">Proyecto · explorar ↗</button><button data-demo="1">Cliente · comprender ↗</button><button data-demo="2">Prospecto · descubrir ↗</button></details><p class="rail-foot">GNX INCEPTION <span>v1.0</span></p></aside><main id="workspace"><div class="topline"><span>Contextos <i>/</i> ${s.demo ? 'Sesión de ejemplo' : 'Nueva posibilidad'}</span><span id="saved">${saved ? '✓ Guardado en este navegador' : 'No guardado'}</span></div><div class="hero"><div><p class="eyebrow purple">DE LA IDEA A LA CLARIDAD</p><h1>${s.view === 'brief' ? 'Tu contexto, listo para compartir.' : count ? 'Tu contexto empieza a tomar forma.' : 'Todo empieza con una conversación.'}</h1><p class="intro">${s.view === 'brief' ? 'Un mapa compartido para decidir qué hacer después.' : 'Trae lo que sabes. Vamos a encontrar juntos lo que importa.'}</p></div><span aria-hidden="true">✳</span></div><div class="workspace-grid"><section class="conversation">${contextControls()}${s.view === 'brief' ? brief() : interview()}<footer><span>◌ A tu ritmo. Puedes volver cuando quieras.</span><span>Motor local · sin IA conectada</span></footer></section><aside class="map"><p class="eyebrow">TU MAPA VIVO</p><h2>Una idea. Más claridad.</h2><div class="clarity"><span>${count === 0 ? 'Un espacio para empezar' : count < 4 ? 'Encontrando el foco' : 'Ya tiene forma'}</span><strong>${count}<small> / ${visibleTopics.length}</small></strong></div><div class="segments" aria-label="${count} de ${visibleTopics.length} dimensiones exploradas">${visibleTopics.map(t => `<i class="${latest(s,t.id) ? 'filled' : ''}"></i>`).join('')}</div><p class="caption">Dimensiones exploradas, no hechos verificados.</p><div class="map-list">${visibleTopics.map((t,i) => `<button data-topic="${t.id}" class="${s.current === t.id && s.view === 'interview' ? 'current' : ''}"><span>${latest(s,t.id) ? '✓' : String(i+1).padStart(2,'0')}</span><div><strong>${t.short}</strong><small>${latest(s,t.id) ? latest(s,t.id)?.uncertain ? 'Por validar' : 'Aportado por ti' : s.skipped.includes(t.id) ? 'Pendiente para después' : 'Por explorar'}</small></div><span>↗</span></button>`).join('')}</div><p class="map-note">↳ ${count ? 'Tu perspectiva ya está aquí. Después podremos contrastarla con quienes viven el proceso.' : 'No hay respuestas perfectas. Empieza con un ejemplo concreto; encontraremos los detalles juntos.'}</p><button class="outline full" data-view="${s.view === 'brief' ? 'interview' : 'brief'}">${s.view === 'brief' ? 'Seguir conversando' : 'Ver mi brief'} ↗</button></aside></div></main></div>`;
  const footerLabel = document.querySelector('footer span:last-child');
  if (footerLabel) footerLabel.textContent = aiLabel;
  if (s.pending && s.aiTurn) { const hint = document.querySelector('.card .hint'); if (hint) hint.textContent = 'Luna organizó tu aporte. Revisa y corrige la síntesis antes de incorporarla al mapa.'; }
  if (aiEnabled && !s.pending && s.view === 'interview') { const caption = document.querySelector('.caption.right'); if (caption) caption.textContent = 'Al continuar, tu texto y contexto previo se envían a OpenAI. Los adjuntos permanecen locales.'; }
  bind();
}
function contextControls() {
  return `<div class="context-controls"><div><label for="context-kind">Contexto ${s.kindConfirmed ? 'seleccionado' : 'sugerido · puedes corregirlo'}</label><select id="context-kind">${Object.entries(kindLabels).map(([id,label]) => `<option value="${id}" ${contextKind(s) === id ? 'selected' : ''}>${label}</option>`).join('')}</select></div><span>${coverage(s).filter(c => c.status === 'reported').length} / ${activeFacets(s).length} aspectos respondidos</span>${aiEnabled ? '<button id="local-mode">Continuar sin IA</button>' : ''}</div>`;
}
function interview() {
  if (s.pending) return `<article class="card"><div class="kicker"><span>✳</span> UNA PAUSA PARA ENTENDERNOS <small>Revisión</small></div><h2>Esto es lo que me llevo.</h2><p class="hint">Conservé tus palabras. Afina lo que quieras antes de incorporarlo al mapa.</p><label for="review">Tu aporte revisado</label><textarea id="review" rows="7" maxlength="12000">${esc(s.pending.text)}</textarea><details class="source"><summary>Ver aporte original</summary><p>${esc(s.pending.original)}</p></details><p class="notice">La revisión confirma tu intención. El contenido sigue siendo reportado por ti, sin corroboración externa.</p><div class="actions"><button id="uncertain">Es una estimación</button><button id="confirm" class="primary">Así es, continuemos ↗</button></div><button id="back" class="back">← Volver a mi respuesta</button></article>`;
  const q = currentQuestion(s);
  const t = {...topics.find(x => x.id === q.topic)!,...q};
  return `<article class="card"><div class="kicker"><span>✳</span> ${esc(t.label.toUpperCase())} <small>${s.claims.length ? 'Sigamos explorando' : 'Empecemos aquí'}</small></div><h2>${esc(t.question)}</h2><p class="hint">${esc(t.hint)}</p><p class="reason">↳ ${esc(t.reason)}</p><label class="sr" for="answer">Tu respuesta</label><textarea id="answer" rows="6" maxlength="12000" placeholder="Escribe como se lo contarías a alguien de tu equipo…">${esc(s.draft)}</textarea><div class="input-tools"><div><button id="record">◉ Grabar voz</button><button id="attach">＋ Adjuntar</button><input id="file" type="file" hidden accept="audio/*,.txt,.md,.csv,.pdf,.docx,.png,.jpg"></div><span id="count">${s.draft.length} / 12 000</span></div><p id="recording" role="status"></p><div class="starters"><p>¿Un punto de partida?</p>${t.starters.map(x => `<button data-starter="${esc(x)}">${esc(x)} ↗</button>`).join('')}</div>${evidence()}<div class="actions"><button id="skip">No lo sé todavía</button><button id="submit" class="primary" ${s.draft.trim() ? '' : 'disabled'}>Encontrar claridad ↗</button></div><p class="caption right">Autosave activo · Ctrl + Enter para continuar</p></article>${s.claims.length ? `<details class="history"><summary>Lo que ya compartiste · ${s.claims.length} aportes</summary>${s.claims.map(c => `<article><small>${esc(topics.find(t => t.id === c.topic)?.short)} · ${c.uncertain ? 'Por validar' : 'Reportado'}</small><p>${esc(c.text)}</p><details><summary>Fuente original</summary><p>${esc(c.original)}</p></details></article>`).join('')}</details>` : '<div class="welcome"><span>✧</span><div><strong>Tu conocimiento es el punto de partida.</strong><p>Texto, notas o una historia reciente. No hace falta hablar de tecnología.</p></div></div>'}`;
}
function evidence() { return s.evidence.length ? `<div class="evidence">${s.evidence.map(f => `<div><span>▧</span><section><strong>${esc(f.name)}</strong><small>${f.status === 'read' ? 'Texto disponible · sin corroboración' : f.type.startsWith('audio/') ? 'Audio local · transcripción manual pendiente' : 'Adjunto local · contenido sin analizar'}</small></section><button data-open="${f.id}" aria-label="Abrir ${esc(f.name)}">Abrir</button><button data-remove="${f.id}" aria-label="Eliminar ${esc(f.name)}">×</button></div>`).join('')}<section id="preview"></section></div>` : ''; }
function brief() {
  const p = packageSession(s);
  const visibleTopics = topics.filter(t => activeFacets(s).some(f => f.topic === t.id));
  return `<article class="card brief"><div class="kicker"><span>✳</span> BRIEF VIVO <small>Por validar</small></div><label for="title">Nombre del contexto</label><input id="title" value="${esc(s.title)}" maxlength="100"><p class="hint">${s.claims.length} aportes · ${s.evidence.length} adjuntos · perspectiva del operador</p>${visibleTopics.map(t => { const c = latest(s,t.id); return `<section class="brief-section"><div><h3>${t.short}</h3><button data-topic="${t.id}" aria-label="Editar ${t.short}">↗</button></div><p class="${c ? '' : 'muted'}">${esc(s.claims.filter(x => x.topic === t.id).map(x => x.text).join('\n\n') || 'Todavía por explorar.')}</p>${c ? `<details class="source"><summary>${c.uncertain ? 'Estimación' : 'Reportado'} · Ver fuente</summary><p>${esc(c.original)}</p></details>` : ''}</section>`; }).join('')}${p.contradictions.map(w => `<p class="notice">${esc(w)}</p>`).join('')}<div class="next"><p class="eyebrow">LA PRÓXIMA MEJOR ACCIÓN</p><h3>${esc(p.handoff.nextBestAction)}</h3><p>${gaps(s).length} aspectos requieren exploración o validación.</p></div><h3 class="small-heading">Tu agente inicial · propuesta</h3><p class="hint">${esc(p.agentDraft.purpose)}</p><p class="hint"><strong>Ayuda inicial:</strong> organizar contexto y preparar preguntas.</p><p class="hint"><strong>Límites:</strong> ${esc(p.agentDraft.boundaries)}</p><label for="destination">Preparar contexto para</label><select id="destination">${[['validation','Validación · completar lo que falta'],['strategy','Estrategia · priorizar la oportunidad'],['builder','Constructor · diseñar la prueba']].map(([v,l]) => `<option value="${v}" ${s.route === v ? 'selected' : ''}>${l}</option>`).join('')}</select><div class="actions"><button class="outline" id="copy">Copiar Markdown</button><button class="outline" id="download-md">Descargar .md</button><button class="primary" id="handoff">Preparar siguiente paso ↗</button></div><p class="caption">El paquete queda guardado aquí. No hay otro agente conectado todavía.</p>${s.route ? '<div class="notice">Markdown preparado para Estrategia y Constructor. <button id="copy-package">Copiar paquete estructurado</button></div>' : ''}<details class="md-preview"><summary>Ver documento Markdown completo · ${gaps(s).length} pendientes</summary><label class="sr" for="markdown">Documento Markdown</label><textarea id="markdown" readonly rows="16">${esc(buildMarkdown(s))}</textarea></details>${evidence()}</article>`;
}
function canNavigate() { if (busy || recorder?.state === 'recording') { notify('Termina la grabación o carga antes de continuar.'); return false; } return true; }
function bind() {
  const on = (id: string, event: string, fn: (ev: Event) => void) => document.getElementById(id)?.addEventListener(event, fn);
  document.querySelectorAll<HTMLElement>('[data-view]').forEach(b => b.onclick = () => { if (!canNavigate()) return; s.view = b.dataset.view as Session['view']; persist(); render(); });
  document.querySelectorAll<HTMLElement>('[data-topic]').forEach(b => b.onclick = () => { if (!canNavigate()) return; if (s.pending || s.draft.trim()) return notify('Guarda o revisa tu aporte antes de cambiar de tema.'); s.current = b.dataset.topic as TopicId; const f = candidateFacets(s).find(c => c.facet.topic === s.current)?.facet ?? activeFacets(s).find(f => f.topic === s.current)!; s.flowQuestion = localQuestion(s,f.id); s.aiTurn = undefined; s.draft = ''; s.view = 'interview'; persist(); render(); });
  on('answer','input', ev => { s.draft = (ev.target as HTMLTextAreaElement).value; persist(); document.querySelector('#count')!.textContent = `${s.draft.length} / 12 000`; (document.querySelector('#submit') as HTMLButtonElement).disabled = !s.draft.trim(); });
  on('answer','keydown', ev => { const k = ev as KeyboardEvent; if ((k.ctrlKey || k.metaKey) && k.key === 'Enter') submit(); });
  document.querySelectorAll<HTMLElement>('[data-starter]').forEach(b => b.onclick = () => { const input = document.querySelector<HTMLTextAreaElement>('#answer')!; input.value += (input.value ? '\n' : '') + b.dataset.starter; input.dispatchEvent(new Event('input')); input.focus(); });
  on('submit','click',submit);
  on('review','input', ev => { if (s.pending) { s.pending.text = (ev.target as HTMLTextAreaElement).value; s.aiTurn = undefined; persist(); } });
  const confirm = (uncertain: boolean) => { if (!s.pending?.text.trim()) return notify('Añade una frase para continuar.'); confirmClaim(s,s.pending.text,uncertain); persist(); render(); notify('Aporte guardado. Tu mapa ganó claridad.'); };
  on('confirm','click',() => confirm(false)); on('uncertain','click',() => confirm(true));
  on('back','click',() => { s.pending = undefined; persist(); render(); });
  on('skip','click',() => { if (!canNavigate()) return; if (s.draft.trim()) return notify('Guarda tu borrador o vacía el campo para dejarlo pendiente.'); deferCurrent(s); persist(); render(); });
  const switchTo = (target: Session) => { s = target; store.activeId = s.id; persist(); render(); };
  on('new','click',() => { if (!canNavigate()) return; const target = createSession(); store.sessions.push(target); switchTo(target); });
  on('sessions','change',ev => { if (!canNavigate()) { (ev.target as HTMLSelectElement).value = s.id; return; } switchTo(store.sessions.find(x => x.id === (ev.target as HTMLSelectElement).value)!); });
  document.querySelectorAll<HTMLElement>('[data-demo]').forEach(b => b.onclick = () => { if (!canNavigate()) return; const target = demoSession(Number(b.dataset.demo)); store.sessions.push(target); switchTo(target); });
  on('title','input',ev => { s.title = (ev.target as HTMLInputElement).value || 'Nuevo contexto'; persist(); });
  on('handoff','click',() => { s.route = (document.querySelector('#destination') as HTMLSelectElement).value; persist(); render(); notify('Contexto preparado para el siguiente paso.'); });
  on('copy-package','click',() => copy(JSON.stringify(packageSession(s),null,2)));
  on('copy','click',() => copy(buildMarkdown(s)));
  on('download-md','click',() => { const url = URL.createObjectURL(new Blob([buildMarkdown(s)],{type:'text/markdown;charset=utf-8'})); const link = document.createElement('a'); link.href = url; link.download = `contexto-360-${s.id.slice(0,8)}.md`; link.click(); setTimeout(() => URL.revokeObjectURL(url),1000); });
  on('context-kind','change',ev => { if (!canNavigate() || s.pending || s.draft.trim()) { notify('Guarda el aporte antes de cambiar el tipo de contexto.'); render(); return; } s.kind = (ev.target as HTMLSelectElement).value as ContextKind; s.kindConfirmed = true; s.aiTurn = undefined; s.flowQuestion = undefined; if (s.claims.length) advance(s); persist(); render(); });
  on('local-mode','click',() => { if (!canNavigate()) return; aiEnabled = false; aiLabel = 'Motor local · sin IA conectada'; render(); notify('Puedes continuar con preguntas locales sin enviar datos a OpenAI.'); });
  on('attach','click',() => { if (canNavigate()) document.getElementById('file')?.click(); });
  on('file','change',ev => { const f = (ev.target as HTMLInputElement).files?.[0]; if (f) void attach(f); });
  on('record','click',record);
  document.querySelectorAll<HTMLElement>('[data-open]').forEach(b => b.onclick = () => { void open(b.dataset.open!); });
  document.querySelectorAll<HTMLElement>('[data-remove]').forEach(b => b.onclick = async () => { if (!canNavigate()) return; try { await deleteFile(b.dataset.remove!); s.evidence = s.evidence.filter(f => f.id !== b.dataset.remove); persist(); render(); notify('Adjunto eliminado del navegador; no se puede deshacer.'); } catch { notify('No se pudo eliminar el archivo.'); } });
}
async function submit() {
  if (!canNavigate() || !s.draft.trim() || s.pending) return;
  s.current = currentQuestion(s).topic;
  const claim = prepareClaim(s,s.draft); persist();
  if (aiEnabled) {
    busy = true;
    const button = document.querySelector<HTMLButtonElement>('#submit');
    const answer = document.querySelector<HTMLTextAreaElement>('#answer');
    if (button) { button.disabled = true; button.textContent = 'Luna está organizando tu aporte…'; }
    if (answer) answer.readOnly = true;
    try {
      const response = await fetch('/api/interview',{method:'POST',headers:{'Content-Type':'application/json'},signal:AbortSignal.timeout(65000),body:JSON.stringify({topic:s.current,answer:claim.original,claims:s.claims.slice(-30).map(c => ({topic:c.topic,text:c.text,questionId:c.questionId})),kind:contextKind(s),kindConfirmed:Boolean(s.kindConfirmed),questionId:currentQuestion(s).id,question:currentQuestion(s).question,candidates:activeFacets(s).filter(f => f.id !== currentQuestion(s).id && !s.deferred?.includes(f.id) && !s.claims.some(c => c.questionId === f.id)).map(f => ({id:f.id,topic:f.topic,question:localQuestion(s,f.id)!.question})),coverage:coverage(s).map(c => ({id:c.facet.id,status:c.status}))})});
      const result = await response.json();
      if (!response.ok) throw Error(result.error || 'No se pudo consultar OpenAI.');
      s.aiTurn = result; claim.text = result.summary; claim.uncertain = claim.uncertain || result.uncertain;
    } catch (error) {
      busy = false; render(); notify(error instanceof Error ? error.message : 'OpenAI no respondió. Tu borrador sigue guardado.'); return;
    }
    busy = false;
  } else s.aiTurn = undefined;
  s.pending = claim; persist(); render(); document.querySelector<HTMLTextAreaElement>('#review')?.focus();
}
async function copy(text: string) { try { await navigator.clipboard.writeText(text); notify('Copiado.'); } catch { notify('El navegador bloqueó el portapapeles. Puedes seleccionar el texto del brief.'); } }
async function attach(file: File) {
  if (file.size > 25 * 1024 * 1024) return notify('Máximo 25 MB por archivo.');
  busy = true;
  try { const id = uid(); await putFile(id,file); const text = /\.(txt|md|csv)$/i.test(file.name) && file.size < 200000 ? await file.text() : undefined; s.evidence.push({ id, name:file.name, type:file.type, size:file.size, text, createdAt:new Date().toISOString(), status:text === undefined ? 'attached' : 'read', topic:s.current }); persist(); render(); notify('Archivo guardado. Ábrelo y describe el fragmento relevante en tu respuesta.'); } catch { notify('No se pudo guardar el adjunto. Tu texto sigue disponible.'); } finally { busy = false; }
}
async function open(id: string) { try { const item = s.evidence.find(f => f.id === id)!; const blob = await getFile(id); if (!blob) return notify('El archivo ya no está disponible.'); const url = URL.createObjectURL(blob); urls.push(url); document.querySelector('#preview')!.innerHTML = `<strong>${esc(item.name)}</strong>${item.type.startsWith('audio/') ? `<audio controls src="${url}"></audio><p>Escucha y escribe el fragmento relevante. Transcripción automática no conectada.</p>` : item.text !== undefined ? `<pre>${esc(item.text)}</pre>` : '<p>Contenido conservado, sin analizar.</p>'}<a href="${url}" download="${esc(item.name)}">Guardar copia del adjunto</a>`; } catch { notify('No se pudo abrir el archivo.'); } }
async function record() {
  if (recorder?.state === 'recording') { recorder.stop(); return; }
  if (busy) return;
  if (!navigator.mediaDevices?.getUserMedia || !window.MediaRecorder) return notify('Puedes adjuntar un audio; este navegador no permite grabar.');
  if (!confirm('Tu voz se guardará solo en este navegador. Puedes reproducirla y eliminarla. ¿Iniciar grabación?')) return;
  busy = true; let stream: MediaStream | undefined;
  try { stream = await navigator.mediaDevices.getUserMedia({ audio:true }); recorder = new MediaRecorder(stream); const chunks: BlobPart[] = []; const start = Date.now(); const type = recorder.mimeType;
    const timer = setInterval(() => { const el = document.querySelector('#recording'); if (el) el.textContent = `Grabando · ${Math.floor((Date.now()-start)/1000)} s`; if (Date.now()-start > 180000 && recorder?.state === 'recording') recorder.stop(); },500);
    recorder.ondataavailable = ev => chunks.push(ev.data);
    recorder.onstop = async () => { clearInterval(timer); stream?.getTracks().forEach(t => t.stop()); recorder = undefined; busy = false; await attach(new File(chunks,`Nota de voz.${type.includes('mp4') ? 'mp4' : 'webm'}`,{ type })); };
    recorder.start(); busy = false; document.querySelector('#record')!.textContent = '■ Detener grabación';
  } catch { stream?.getTracks().forEach(t => t.stop()); busy = false; notify('No se pudo acceder al micrófono. Revisa el permiso o adjunta un audio.'); }
}
window.addEventListener('beforeunload',ev => { if (!saved || busy || recorder?.state === 'recording') { ev.preventDefault(); ev.returnValue = ''; } });
render();
void fetch('/health').then(r => r.json()).then(config => { aiEnabled = config.mode === 'openai'; aiLabel = aiEnabled ? `${config.model} · ${config.effort}` : 'Motor local · sin IA conectada'; render(); }).catch(() => { aiLabel = 'Motor local · conexión no disponible'; render(); });
